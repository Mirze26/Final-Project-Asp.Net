@using Payne.Areas.Admin.Views.Color;
@model List<ColorIndexVM>

@{
    ViewData["Title"] = "Color";
}


<div class="container my-5">
    <a class="btn btn-success" style="float: right; position:absolute; right:666px;top:98px" asp-action="Create">Create</a>

    <div class="table-responsive">
        <table class="table table-striped"style="width:400px; position:absolute;right:737px" >
            <thead>
                <tr>
                    <th>
                        Name
                    </th>

                    <th>
                        Operations

                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var color in Model)
                {
                    <tr>
                        <td>
                            @color.Name
                        </td>


                        <td style="width:145px">

                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@color.Id">Edit</a>
                            <form style="display: inline-block;" asp-action="Delete" asp-route-id="@color.Id" method="post">
                                <button type="submit" class="btn btn-outline-danger">Delete</button>
                            </form>

                        </td>

                    </tr>
                }


            </tbody>
        </table>
    </div>

</div>

